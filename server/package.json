{
  "name": "app-store-server-tests",
  "version": "1.0.0",
  "description": "企业内部应用发布平台测试套件",
  "main": "index.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:api": "vitest run api.test.js",
    "test:consistency": "vitest run consistency.test.js"
  },
  "devDependencies": {
    "vitest": "^1.2.0",
    "@vitest/coverage-v8": "^1.2.0",
    "supertest": "^6.3.4",
    "node-fetch": "^3.3.2"
  },
  "dependencies": {
    "uuid": "^9.0.0"
  }
}
