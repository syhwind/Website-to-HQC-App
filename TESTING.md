# 企业内部应用发布平台 - 测试文档

## 测试概述

本测试套件用于验证企业内部应用发布平台的功能完整性和数据一致性，特别关注新发布应用从编辑状态到展示页面的数据一致性。

## 测试架构

```
项目根目录
├── 前端测试
│   ├── vitest.config.ts          # 前端测试配置
│   ├── components.test.tsx       # 前端组件和集成测试
│   └── test/setup.ts            # 测试环境配置
│
└── server/                      # 后端测试
    ├── package.json             # 后端测试依赖
    ├── vitest.config.js         # 后端测试配置
    ├── api.test.js              # API 完整功能测试
    └── consistency.test.js      # 数据一致性专项测试
```

## 功能测试覆盖范围

### 1. 新应用发布流程测试

| 测试项 | 测试内容 | 状态 |
|--------|---------|------|
| 创建应用 | POST /api/apps 创建新应用 | ✅ |
| 获取应用详情 | GET /api/apps/:id | ✅ |
| 更新应用信息 | PUT /api/apps/:id | ✅ |
| 编辑前后数据一致性 | 验证更新后的字段正确保存 | ✅ |
| 列表页可见性 | 新应用在列表页正确显示 | ✅ |

### 2. 数据一致性专项测试

| 场景 | 验证内容 |
|------|---------|
| 全生命周期 | 应用创建 → 编辑 → 删除全流程数据一致 |
| 字段完整性 | 所有字段在编辑过程中保持完整 |
| 未修改字段 | 编辑时未提供的字段保持原样 |
| 边界条件 | 长文本、大数组等边界值测试 |
| 并发操作 | 多用户同时操作时数据一致性 |
| 状态切换 | active ↔ inactive 切换的数据一致性 |

### 3. 关联功能测试

| 功能 | 测试项 |
|------|-------|
| 评价 | 评价提交、展示、平均评分计算 |
| 收藏 | 收藏/取消、计数、列表展示 |
| 浏览量 | 浏览量正确统计 |
| 状态管理 | 上下架状态正确处理 |

## 快速开始

### 安装测试依赖

```bash
# 前端
npm install

# 后端
cd server
npm install
```

### 运行测试

#### 1. 使用交互式脚本（推荐）

```bash
# 给脚本添加执行权限
chmod +x run-tests.sh

# 运行
./run-tests.sh
```

#### 2. 手动运行测试

**后端测试:**

```bash
cd server

# 运行所有测试
npm run test

# 只运行 API 测试
npm run test:api

# 只运行数据一致性测试
npm run test:consistency

# 查看测试覆盖率
npm run test:coverage
```

**前端测试:**

```bash
# 运行所有前端测试
npm run test

# 只运行组件测试
npm run test:components

# 查看测试覆盖率
npm run test:coverage
```

## 测试用例详解

### API 测试 (api.test.js)

#### 测试组 1: 分类管理
- 验证能否正确获取分类列表

#### 测试组 2: 新应用发布完整流程
- 2.1 创建应用
- 2.2 获取刚创建的应用
- 2.3 更新应用
- 2.4 验证编辑后数据一致性
- 2.5 验证列表页可见性
- 2.6-2.9 状态管理测试

#### 测试组 3: 数据一致性深度验证
- 3.1-3.6 完整字段验证、未修改字段保持、关联数据一致性等

#### 测试组 4: 评价功能
- 4.1-4.3 评价提交、展示、评分计算

#### 测试组 5: 收藏功能
- 5.1-5.4 收藏/取消收藏、列表展示

### 数据一致性专项测试 (consistency.test.js)

#### 场景一: 完整应用生命周期
- 测试完整的应用生命周期数据一致性

#### 场景二: 多用户并发操作
- 验证浏览量、收藏计数的一致性

#### 场景三: 数据完整性边界
- 验证长文本、大数组等边界条件

#### 场景四: 状态一致性
- 验证状态切换时的数据完整性

## 运行前准备

### 1. 启动后端服务

确保后端服务正在运行:

```bash
cd server
npm run dev
```

后端服务默认地址: `http://localhost:3001`

### 2. 数据库连接

确保达梦数据库已正常运行，连接配置在:
`server/src/db/connection.js`

## 测试数据说明

测试过程中会创建临时数据，测试完成后会自动清理。

如果测试中途失败，可能需要手动清理数据库中的测试数据。

## 重点验证项目

### 编辑前后数据一致性

这是本测试套件的核心关注点:

1. **字段完整性** - 所有字段在编辑操作后都保持完整
2. **未修改字段** - 编辑时未提供的字段保持原始值
3. **关联数据** - 数组、嵌套对象等结构保持一致
4. **统计字段** - 浏览量、收藏数、评分等正确更新
5. **可见性** - 应用在不同页面（列表、详情）数据一致

### 状态切换一致性

1. **active → inactive** - 下架后应用不在列表页显示
2. **inactive → active** - 重新上架后应用恢复可见
3. **数据完整性** - 状态切换期间其他数据不被破坏

## 故障排除

### 问题: 测试失败说无法连接到后端

**解决方法:** 确保后端服务正在运行
```bash
cd server && npm run dev
```

### 问题: 数据库连接错误

**解决方法:** 检查数据库配置和连接状态
- 查看 `server/src/db/connection.js`
- 确保达梦数据库服务正在运行

### 问题: 测试依赖未安装

**解决方法:** 重新安装依赖
```bash
# 前端
npm install

# 后端
cd server && npm install
```

## 扩展测试

### 添加新测试

1. **后端测试**: 在 `server/` 目录下添加新的 `.test.js` 文件
2. **前端测试**: 在根目录或 `test/` 目录下添加 `.test.tsx` 文件

### 配置修改

- 后端测试配置: `server/vitest.config.js`
- 前端测试配置: `vitest.config.ts`
- API 基础 URL: 可通过环境变量 `API_BASE_URL` 配置

## 持续集成建议

在 CI/CD 流程中:

```yaml
# 示例 CI 步骤
- 安装依赖
- 启动后端服务
- 运行 API 测试
- 运行数据一致性测试
- 生成测试报告
- 停止后端服务
```

## 支持和反馈

如有问题或建议，请参考项目文档或联系开发团队。
